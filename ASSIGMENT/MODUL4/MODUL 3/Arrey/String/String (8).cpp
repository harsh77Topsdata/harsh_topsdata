#include<stdio.h>
#include<string.h>

main()
{
	char a[100];
	int i,len=0,maximum;
	
	printf("Enter the intiger :");
	gets(a);
	
	if(len='\0')
	{
		printf("%c",a[i]);
		len++;
	}
	
	printf("\nthe maximum character in a string %d",len);
	
}
